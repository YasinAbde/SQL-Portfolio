WITH CTE AS
(SELECT *,
ROW_NUMBER() OVER(ORDER BY LAT_N ASC) rn,
ROW_NUMBER() OVER(ORDER BY LAT_N DESC) dn
FROM STATION)
SELECT CAST(AVG(LAT_N) AS DECIMAL (9,4)) FROM CTE
WHERE ABS(CAST(rn AS SIGNED) - CAST(dn AS SIGNED)) <= 1;